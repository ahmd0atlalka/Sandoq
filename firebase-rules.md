# قواعد الأمان لـ Firebase Realtime Database

## المشكلة
يظهر تحذير أمني في Firebase Console: "Your security rules are defined as public"

## الحل

### الخطوة 1: تعديل قواعد الأمان
1. اذهب إلى [Firebase Console](https://console.firebase.google.com)
2. اختر مشروعك `sandoq-b1c38`
3. اذهب إلى **Realtime Database**
4. اضغط على تبويب **Rules**
5. استبدل القواعد الحالية بـ:

```json
{
  "rules": {
    ".read": true,
    ".write": true
  }
}
```

### الخطوة 2: تأكيد التغييرات
1. اضغط على **Publish**
2. انتظر لحظة حتى يتم تطبيق القواعد

### الخطوة 3: اختبار الاتصال
1. شغل التطبيق: `npm start`
2. اضغط على "اختبار الاتصال"
3. يجب أن يعمل الآن بشكل صحيح

## ملاحظات مهمة

⚠️ **تحذير أمني**: هذه القواعد تسمح بالوصول العام للقراءة والكتابة. 
في البيئة الإنتاجية، يجب استخدام قواعد أمان أكثر صرامة.

### قواعد أمان أكثر أماناً (اختياري):
```json
{
  "rules": {
    ".read": "auth != null",
    ".write": "auth != null"
  }
}
```

## بيانات الاختبار المتوقعة

بناءً على الصورة، يجب أن تكون البيانات بهذا الشكل:
```json
{
  "0": {
    "Code": "A-1",
    "IS_Participated": 1,
    "Is_received": 1,
    "city": "ابطن",
    "email": "nihaya.7833@gmail.com",
    "full name": "يزن محمد احمد عماريه",
    "id": 215932286,
    "n1": "يزن"
  }
}
```

## أمثلة للبحث
- رقم المجموعة: `A-1`
- رقم الهوية: `215932286` 